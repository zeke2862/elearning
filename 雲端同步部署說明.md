# 雲端同步部署說明

這份是給你自己照著做一次的設定步驟，做完之後「在家練習」的資料才會真的
送得到學校。全部只要做一次，之後都是自動運作。

## 這個機制在做什麼（先看懂，設定會比較安心）

```
學生在家 (瀏覽器)  --送資料-->  Google 試算表 (雲端中繼站)
                                        |
                                        | 老師電腦按「從雲端匯入」時才會來抓
                                        v
老師電腦 (連得到校內主機 + 連得到網路)  --寫入-->  校內資料庫 (server.js/database.js)
```

- 學生在家練習，資料**連不到**你學校的 192.168.1.1 主機，所以會先寄到一個
  Google 試算表（暫存信箱）。
- 你（或任何連得到網路又連得到校內主機的電腦，通常就是你的教師電腦）**手
  動按一個按鈕**，把試算表裡還沒處理的資料抓下來，寫進學校主機的資料庫。
- 這不是即時的，是「你按了才會同步」——想更頻繁，就常按幾次；不按也完全
  不影響學生原本在學校練習、在家練習照常運作，只是在家的成績會晚一點才
  進到校內資料庫。

## 步驟一：建立 Google 試算表 + 部署 Apps Script

1. 到 [sheets.google.com](https://sheets.google.com) 用你的 Google 帳號新增
   一份空白試算表，取個名字（例如「鍵位特工-雲端同步」）。
2. 上方選單：**擴充功能 → Apps Script**，會開一個新分頁，裡面有個空的
   `Code.gs` 檔案。
3. 把裡面原本的範例程式碼全部刪掉，貼上專案附的 **`雲端同步-Code.gs`**
   這份檔案的完整內容。
4. 按左上角儲存（磁片圖示），專案取個名字都可以。
5. 按右上角藍色的 **部署 → 新增部署作業**。
   - 類型選「網頁應用程式」（Web app）。
   - 「執行身分」選 **我**（你自己的帳號）。
   - 「誰可以存取」選 **所有人**（Anyone）——這一步容易漏掉，沒選對的話
     學生的瀏覽器會被擋下來，一定要選「所有人」而不是「知道這個帳號的使
     用者」那種需要 Google 登入的選項。
   - 按部署，過程中會跳出「授權」的畫面，照著點允許就好（是在授權你自己
     的 Apps Script 能寫入你自己的試算表，不會有任何資料外流到別的地方）。
6. 部署完成後會給你一段網址，長得像：
   ```
   https://script.google.com/macros/s/AKfycb.../exec
   ```
   把這段網址整個複製下來，接下來兩個地方要用到。

## 步驟二：把網址填進學生端（sync-client.js）

打開 `sync-client.js`，找到最上面這一行：

```js
const CLOUD_RELAY_URL = ""; // 例如 "https://script.google.com/macros/s/xxxxx/exec"
```

把引號中間貼上你步驟一拿到的網址，存檔。這支 `sync-client.js` 要跟
`login.html`、看打測驗、ETP 測驗放在同一個資料夾，這樣三邊才會都讀到你填
的網址。

**這一步之前，同步功能全部都是安靜關閉的**（`CLOUD_RELAY_URL` 是空字串時，
`login.html` 選單頁不會顯示「立即同步」按鈕），所以你可以先把其他東西上線
測試，等真的要開這個功能了再回來填。

## 步驟三：把同一個網址填進老師端（admin.html）

打開 `admin.html`，登入後台，捲到最下面「六、雲端同步」，把同一段網址貼進
輸入框，按「記住這個網址」。以後開這個頁面會自動帶回來，不用每次都貼。

## 步驟四：實際測試一次

1. 找一台**不在學校網路**的裝置（例如你自己手機的行動網路，關掉 WiFi），
   打開 `login.html`，用一個測試學號登入（會變成「本機模式」，這是正常
   的，因為連不到校內主機）。
2. 進去隨便玩一下塔防、或考一次 ETP／看打。
3. 回到選單頁，按「☁️ 立即同步雲端」，應該會顯示「✅ 已同步到雲端」。
4. 打開你步驟一建立的那份 Google 試算表，應該會多一列資料，狀態欄是
   `pending`。
5. 在學校電腦（或任何連得到 192.168.1.1 校內主機的電腦）打開 `admin.html`
   的「六、雲端同步」，按「☁️ 從雲端匯入待處理資料」。
6. 應該會顯示「匯入完成：成功 1 筆」，回頭看那份 Google 試算表，剛剛那列
   狀態會變成 `done`。
7. 用同一個測試學號在學校電腦登入 `login.html`，應該會看到剛剛在「家」
   賺到的 GBit 也帶回來了。

以上都成功，代表整個同步鏈路是通的，之後不用再測試，平常只要記得三不五
時按一下「從雲端匯入」就好。

## 之後要注意的小事

- **多久按一次「從雲端匯入」看你**：這個系統不是即時同步，是你主動按才會
  動作。建議每天/每週固定按個幾次，或是段考前多按幾次確保資料都到齊。
- **這份試算表不需要手動整理**：已處理的資料會自動標成 `done`，之後匯入
  只會抓 `pending` 的，不會重複匯入。如果哪天列數太多想清乾淨，把 `done`
  的列整批刪掉即可，程式不會受影響。
- **換學校網址**：如果哪天校內主機的 IP 不是 192.168.1.1 了，記得同時改
  `sync-client.js` 相關檔案裡引用學校主機的地方（`login.html` 的
  `SCHOOL_HOST`、ETP 測驗檔案裡的 `SERVER_URL`）。
- **這個機制目前只有「家 → 校」單向自動**：學校電腦練習的進度本來就直接
  寫進校內資料庫，不需要另外同步；「校 → 家」（同一個學生換一台學校電腦
  也能看到最新進度）已經在登入時做了（`login.html` 登入成功會拉一次
  `/get-save`），但如果學生在同一天內用了兩台不同的學校電腦，中間沒有重
  新登入的話不會即時互通，這是目前的已知限制。
